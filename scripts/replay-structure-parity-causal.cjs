require("dotenv").config({ path: require("path").resolve(__dirname, "..", ".env.local") });
const fs=require("fs"), path=require("path"), {Pool}=require("pg");
const {structureFeature,detectCausalStructure}=require("../apps/engine/dist/features/structure.js");
const {pivotFeature}=require("../apps/engine/dist/features/pivot.js");
const symbols=(process.env.PARITY_SYMBOLS||"EURUSD,GBPJPY,XAUUSD").split(",").map(s=>s.trim().toUpperCase()).filter(Boolean);const days=Number(process.env.PARITY_DAYS||30);const tf=process.env.PARITY_TF||"1h";const tableByTf={"5m":"market.candles_5m_canonical","15m":"market.candles_15m_canonical","1h":"market.candles_1h_canonical"};if(!Object.hasOwn(tableByTf,tf))throw new Error(`Unsupported PARITY_TF: ${tf}`);const candleTable=tableByTf[tf];const output=path.resolve(process.cwd(),process.env.PARITY_OUTPUT||`reports/structure-parity-${tf}-causal.json`);
const pool=new Pool({host:process.env.TM_DB_HOST||"localhost",port:Number(process.env.TM_DB_PORT||5432),database:process.env.TM_DB_NAME||"tradzfx_v2",user:process.env.TM_DB_USER||"postgres",password:process.env.TM_DB_PASSWORD,max:1});const htf={direction:"neutral",confidence:0,state:"BLOCK",score:0,reason:"parity_read_only"};
(async()=>{const results=[];for(const symbol of symbols){const client=await pool.connect();try{await client.query("BEGIN READ ONLY");const r=await client.query(`SELECT ts,o,h,l,c,COALESCE(v,0) v FROM ${candleTable} WHERE symbol=$1 AND ts>=now()-($2::int*interval '1 day')-interval '20 hours' ORDER BY ts`,[symbol,days]);const candles=r.rows.map(x=>({symbol,ts:new Date(x.ts),o:+x.o,h:+x.h,l:+x.l,c:+x.c,v:+x.v}));if(!candles.length){results.push({symbol,status:"NO_CANDLES"});await client.query("ROLLBACK");continue}const pivot=pivotFeature.compute({candles},{tf});const input={candles,features_pivot:pivot,features_atr:{values:[{period:14,value:0}]},features_htf_bias:htf};const endTs=candles.at(-1).ts;const old=structureFeature.compute(input,{tf,endTs});const neu=detectCausalStructure(input,{symbol,tf,endTs});results.push({symbol,status:"OK",warning:"Pivots recomputed from candles",candles:candles.length,pivots:pivot.pivots.length,productionEvents:old.events.length,causalEvents:neu.events.length,causalTypes:neu.events.reduce((a,e)=>(a[e.eventType]=(a[e.eventType]||0)+1,a),{})});await client.query("ROLLBACK")}finally{client.release()}}fs.mkdirSync(path.dirname(output),{recursive:true});fs.writeFileSync(output,JSON.stringify({mode:"read_only",tf,days,results},null,2));console.log(JSON.stringify({output,results},null,2));await pool.end()})().catch(e=>{console.error(e);process.exitCode=1})
