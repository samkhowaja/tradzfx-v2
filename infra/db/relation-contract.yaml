version: 1
scope:
  schemas: [public, ops]
  expectedRelationCount: 80
  generatedFrom: DATABASE_RELATION_PURPOSE_ACCESS_AUDIT_2026-07-17
  reviewedAt: 2026-07-17
  policy:
    unknownRelation: error
    missingCanonicalRelation: error
    missingDerivedRelation: error
    missingLegacyRelation: warning
    presentRetiredRelation: error
relations:
  ops.feature_pipeline_symbols: { kind: table, domain: ops, classification: configuration, grain: one feature-compute policy per symbol, status: canonical, ownerRole: tradzfx_engine, pitPolicy: operational-configuration, retention: current-plus-audit }
  public.ai_narratives: { kind: table, domain: analysis, classification: derived, grain: one narrative per evaluated entity/version, status: canonical, ownerRole: tradzfx_backtest, pitPolicy: source-run-bound, retention: retain-with-source-run }
  public.analysis_run: { kind: table, domain: analysis, classification: ledger, grain: one analysis execution, status: canonical, ownerRole: tradzfx_backtest, pitPolicy: immutable-run, retention: long-term }
  public.analysis_signal: { kind: table, domain: analysis, classification: fact, grain: one signal observation per analysis run, status: canonical, ownerRole: tradzfx_backtest, pitPolicy: immutable-run, retention: long-term }
  public.analysis_trade: { kind: table, domain: analysis, classification: fact, grain: one simulated trade per analysis run, status: canonical, ownerRole: tradzfx_backtest, pitPolicy: immutable-run, retention: long-term }
  public.backtest_results: { kind: table, domain: analysis, classification: fact, grain: one backtest result/trade row, status: canonical, ownerRole: tradzfx_backtest, pitPolicy: immutable-run, retention: long-term }
  public.backtest_runs: { kind: table, domain: analysis, classification: ledger, grain: one backtest execution, status: canonical, ownerRole: tradzfx_backtest, pitPolicy: immutable-run, retention: long-term }
  public.calibration_tuning: { kind: table, domain: analysis, classification: derived, grain: one calibration candidate/result, status: canonical, ownerRole: tradzfx_backtest, pitPolicy: source-run-bound, retention: long-term }
  public.candle_coverage: { kind: table, domain: raw, classification: observation, grain: one coverage observation per symbol/source/range, status: canonical, ownerRole: tradzfx_ingest, pitPolicy: not-signal-input, retention: rolling-plus-summary }
  public.candle_quality: { kind: table, domain: raw, classification: observation, grain: one quality observation per symbol/source/range, status: canonical, ownerRole: tradzfx_ingest, pitPolicy: not-signal-input, retention: rolling-plus-summary }
  public.candles_15m: { kind: view, domain: market, classification: projection, grain: one canonical 15m candle per symbol/broker/bucket, status: canonical, ownerRole: tradzfx_owner, pitPolicy: anchor-bounded, retention: timescale-managed }
  public.candles_1d_ny: { kind: view, domain: market, classification: projection, grain: one 21:00-UTC-boundary daily candle per symbol/broker/bucket, status: auxiliary, ownerRole: tradzfx_owner, pitPolicy: anchor-bounded, retention: timescale-managed }
  public.candles_1d_utc: { kind: view, domain: market, classification: projection, grain: one UTC daily candle per symbol/broker/bucket, status: canonical, ownerRole: tradzfx_owner, pitPolicy: anchor-bounded, retention: timescale-managed }
  public.candles_1h: { kind: view, domain: market, classification: projection, grain: one canonical 1h candle per symbol/broker/bucket, status: canonical, ownerRole: tradzfx_owner, pitPolicy: anchor-bounded, retention: timescale-managed }
  public.candles_1m: { kind: table, domain: raw, classification: source, grain: one M1 candle per symbol/broker/timestamp, status: canonical, ownerRole: tradzfx_ingest, pitPolicy: anchor-bounded, retention: long-term-compressed }
  public.candles_4h: { kind: view, domain: market, classification: projection, grain: one canonical 4h candle per symbol/broker/bucket, status: canonical, ownerRole: tradzfx_owner, pitPolicy: anchor-bounded, retention: timescale-managed }
  public.candles_5m: { kind: view, domain: market, classification: projection, grain: one canonical 5m candle per symbol/broker/bucket, status: canonical, ownerRole: tradzfx_owner, pitPolicy: anchor-bounded, retention: timescale-managed }
  public.decision_trace: { kind: table, domain: strategy, classification: audit, grain: one decision/gate trace step, status: canonical, ownerRole: tradzfx_strategy, pitPolicy: immutable-decision, retention: partitioned-long-term }
  public.feature_cache: { kind: table, domain: ops, classification: cache, grain: one feature result per deterministic input hash, status: canonical, ownerRole: tradzfx_engine, pitPolicy: rebuildable-not-authoritative, retention: ttl }
  public.feature_config_snapshot: { kind: table, domain: strategy, classification: snapshot, grain: one immutable feature configuration hash, status: canonical, ownerRole: tradzfx_strategy, pitPolicy: immutable-snapshot, retention: long-term }
  public.feature_jobs: { kind: table, domain: ops, classification: queue, grain: one requested feature job, status: provisional, ownerRole: tradzfx_engine, pitPolicy: operational-only, retention: short }
  public.feature_producer_runs: { kind: table, domain: ops, classification: ledger, grain: one producer execution per feature/symbol/tf/window, status: canonical, ownerRole: tradzfx_engine, pitPolicy: operational-lineage, retention: partitioned-plus-summary }
  public.features_atr: { kind: table, domain: market, classification: state-feature, grain: one ATR parameterization per symbol/tf/anchor, status: canonical, ownerRole: tradzfx_engine, pitPolicy: anchor-bounded, retention: feature-history }
  public.features_bias: { kind: table, domain: market, classification: state-feature, grain: one local bias per symbol/tf/anchor, status: canonical, ownerRole: tradzfx_engine, pitPolicy: anchor-bounded, retention: feature-history }
  public.features_bollinger: { kind: table, domain: market, classification: state-feature, grain: one Bollinger parameterization per symbol/tf/anchor, status: canonical, ownerRole: tradzfx_engine, pitPolicy: anchor-bounded, retention: feature-history }
  public.features_candle_pattern: { kind: table, domain: market, classification: event-feature, grain: one candle-pattern event per symbol/tf/anchor/pattern, status: canonical, ownerRole: tradzfx_engine, pitPolicy: anchor-bounded, retention: feature-history }
  public.features_correlation: { kind: table, domain: market, classification: state-feature, grain: one pair correlation parameterization per anchor, status: canonical, ownerRole: tradzfx_engine, pitPolicy: anchor-bounded, retention: feature-history }
  public.features_direction_state: { kind: table, domain: market, classification: projection, grain: one consolidated direction state per symbol/tf/anchor, status: canonical, ownerRole: tradzfx_engine, pitPolicy: anchor-bounded, retention: feature-history }
  public.features_displacement: { kind: table, domain: market, classification: event-feature, grain: one displacement event per symbol/tf/source anchor/discriminator, status: canonical, ownerRole: tradzfx_engine, pitPolicy: anchor-bounded, retention: feature-history }
  public.features_eq_liquidity: { kind: table, domain: market, classification: event-feature, grain: one equal-liquidity event per immutable source lineage, status: canonical, ownerRole: tradzfx_engine, pitPolicy: lifecycle-as-of-anchor, retention: feature-history }
  public.features_fvg_backup: { kind: table, domain: archive, classification: migration-snapshot, grain: legacy FVG event copy, status: legacy, ownerRole: tradzfx_owner, pitPolicy: never-runtime, retention: expire-after-reconciliation }
  public.features_fvg: { kind: table, domain: archive, classification: retired, grain: retired standalone FVG event, status: retired, ownerRole: tradzfx_owner, pitPolicy: never-runtime, retention: remove-after-proof }
  public.features_htf_bias: { kind: table, domain: market, classification: state-feature, grain: one cross-timeframe bias per symbol/tf/anchor, status: canonical, ownerRole: tradzfx_engine, pitPolicy: anchor-bounded, retention: feature-history }
  public.features_ifvg: { kind: table, domain: market, classification: event-feature, grain: one inverse-FVG event per immutable source lineage, status: canonical, ownerRole: tradzfx_engine, pitPolicy: lifecycle-as-of-anchor, retention: feature-history }
  public.features_indicator: { kind: table, domain: market, classification: state-feature, grain: one generic indicator parameterization per symbol/tf/anchor, status: provisional, ownerRole: tradzfx_engine, pitPolicy: anchor-bounded, retention: feature-history }
  public.features_keltner: { kind: table, domain: market, classification: state-feature, grain: one Keltner parameterization per symbol/tf/anchor, status: canonical, ownerRole: tradzfx_engine, pitPolicy: anchor-bounded, retention: feature-history }
  public.features_liquidity_pools: { kind: table, domain: market, classification: event-feature, grain: one liquidity-pool event per immutable source lineage, status: canonical, ownerRole: tradzfx_engine, pitPolicy: lifecycle-as-of-anchor, retention: feature-history }
  public.features_moving_average: { kind: table, domain: market, classification: state-feature, grain: one moving-average parameterization per symbol/tf/anchor, status: canonical, ownerRole: tradzfx_engine, pitPolicy: anchor-bounded, retention: feature-history }
  public.features_opening_range: { kind: table, domain: market, classification: session-feature, grain: one opening-range state per symbol/session/date/profile, status: canonical, ownerRole: tradzfx_engine, pitPolicy: anchor-bounded, retention: feature-history }
  public.features_order_block: { kind: table, domain: market, classification: event-feature, grain: one order-block event per immutable source lineage, status: canonical, ownerRole: tradzfx_engine, pitPolicy: lifecycle-as-of-anchor, retention: feature-history }
  public.features_pivot: { kind: table, domain: market, classification: event-feature, grain: one pivot event per symbol/tf/source anchor/discriminator, status: canonical, ownerRole: tradzfx_engine, pitPolicy: anchor-bounded, retention: feature-history }
  public.features_pricing: { kind: table, domain: market, classification: state-feature, grain: one pricing state per symbol/tf/anchor/profile, status: canonical, ownerRole: tradzfx_engine, pitPolicy: anchor-bounded, retention: feature-history }
  public.features_session_hl: { kind: table, domain: market, classification: session-feature, grain: one session high-low state per symbol/session/date, status: canonical, ownerRole: tradzfx_engine, pitPolicy: anchor-bounded, retention: feature-history }
  public.features_session: { kind: table, domain: market, classification: session-feature, grain: one session membership/state per symbol/tf/anchor, status: canonical, ownerRole: tradzfx_engine, pitPolicy: anchor-bounded, retention: feature-history }
  public.features_spread: { kind: table, domain: market, classification: state-feature, grain: one spread observation per symbol/broker/anchor, status: canonical, ownerRole: tradzfx_engine, pitPolicy: anchor-bounded, retention: feature-history }
  public.features_structure: { kind: table, domain: market, classification: event-feature, grain: one structure event per immutable source lineage, status: canonical, ownerRole: tradzfx_engine, pitPolicy: lifecycle-as-of-anchor, retention: feature-history }
  public.features_sweep: { kind: table, domain: market, classification: event-feature, grain: one liquidity-sweep event per immutable source lineage, status: canonical, ownerRole: tradzfx_engine, pitPolicy: lifecycle-as-of-anchor, retention: feature-history }
  public.features_time_of_day_edge: { kind: table, domain: market, classification: derived-feature, grain: one time-of-day edge statistic per symbol/profile/bucket/version, status: canonical, ownerRole: tradzfx_engine, pitPolicy: source-window-bound, retention: feature-history }
  public.features_zone_clean: { kind: table, domain: archive, classification: migration-snapshot, grain: ungoverned copy of feature-zone events, status: legacy, ownerRole: tradzfx_owner, pitPolicy: never-runtime, retention: expire-after-reconciliation }
  public.features_zone_retest: { kind: table, domain: market, classification: event-surface, grain: one zone retest observation per zone/candle, status: canonical, ownerRole: tradzfx_engine, pitPolicy: anchor-bounded, retention: partitioned-feature-history }
  public.features_zone: { kind: table, domain: market, classification: event-feature, grain: one zone/FVG event per immutable source lineage, status: canonical, ownerRole: tradzfx_engine, pitPolicy: lifecycle-as-of-anchor, retention: feature-history }
  public.lifecycle_refresh_state_tf: { kind: table, domain: archive, classification: retired, grain: retired per-symbol/tf lifecycle checkpoint, status: retired, ownerRole: tradzfx_owner, pitPolicy: never-runtime, retention: remove-after-reader-cutover }
  public.lifecycle_refresh_state: { kind: table, domain: ops, classification: state, grain: one checkpoint per symbol/lifecycle producer, status: canonical, ownerRole: tradzfx_lifecycle, pitPolicy: operational-only, retention: current-state }
  public.live_deployment: { kind: table, domain: strategy, classification: state, grain: one live deployment/version, status: canonical, ownerRole: tradzfx_strategy, pitPolicy: snapshot-linked, retention: long-term }
  public.live_fill: { kind: table, domain: execution, classification: audit, grain: one immutable live fill observation, status: canonical, ownerRole: tradzfx_execution, pitPolicy: immutable-event, retention: long-term }
  public.live_order: { kind: table, domain: execution, classification: audit, grain: one deployment/signal order snapshot, status: canonical, ownerRole: tradzfx_execution, pitPolicy: immutable-projection, retention: long-term }
  public.live_signal_rejection: { kind: table, domain: strategy, classification: audit, grain: one rejected live signal observation, status: canonical, ownerRole: tradzfx_strategy, pitPolicy: immutable-decision, retention: partitioned-plus-summary }
  public.live_signal: { kind: table, domain: strategy, classification: audit, grain: one deployment-bound live signal, status: canonical, ownerRole: tradzfx_strategy, pitPolicy: immutable-decision, retention: long-term }
  public.market_levels_view: { kind: view, domain: market, classification: projection, grain: one projected market level per source feature event, status: canonical, ownerRole: tradzfx_owner, pitPolicy: source-feature-policy, retention: derived }
  public.market_levels: { kind: table, domain: market, classification: materialized-projection, grain: one normalized market level per level hash, status: provisional, ownerRole: tradzfx_engine, pitPolicy: unresolved, retention: proof-required }
  public.market_volatility_profile: { kind: table, domain: market, classification: state, grain: one volatility profile per symbol/tf/profile/version, status: canonical, ownerRole: tradzfx_engine, pitPolicy: source-window-bound, retention: current-plus-history }
  public.market_zone_objects_active: { kind: view, domain: market, classification: projection, grain: one active zone object, status: provisional, ownerRole: tradzfx_owner, pitPolicy: source-feature-policy, retention: derived }
  public.market_zone_objects: { kind: table, domain: market, classification: materialized-projection, grain: one bucketed zone object per grouping/version, status: provisional, ownerRole: tradzfx_engine, pitPolicy: source-watermark-required, retention: rebuildable }
  public.mt5_terminals: { kind: table, domain: execution, classification: state, grain: one terminal/account heartbeat state, status: canonical, ownerRole: tradzfx_execution, pitPolicy: operational-only, retention: current-plus-audit }
  public.orders: { kind: table, domain: execution, classification: authority, grain: one executable order lifecycle, status: canonical, ownerRole: tradzfx_execution, pitPolicy: event-lineage, retention: long-term }
  public.pipeline_health: { kind: view, domain: ops, classification: projection, grain: one pipeline health row per configured pipeline entity, status: canonical, ownerRole: tradzfx_owner, pitPolicy: operational-only, retention: derived }
  public.pipeline_trigger_state: { kind: table, domain: ops, classification: state, grain: one trigger checkpoint per pipeline key, status: canonical, ownerRole: tradzfx_engine, pitPolicy: operational-only, retention: current-state }
  public.position_commands: { kind: table, domain: execution, classification: queue, grain: one idempotent position-management command, status: canonical, ownerRole: tradzfx_execution, pitPolicy: event-lineage, retention: long-term }
  public.processed_ea_events: { kind: table, domain: execution, classification: ledger, grain: one idempotently received EA event, status: canonical, ownerRole: tradzfx_execution, pitPolicy: immutable-event, retention: long-term }
  public.risk_state: { kind: table, domain: execution, classification: state, grain: one current risk state per governed scope, status: canonical, ownerRole: tradzfx_execution, pitPolicy: current-plus-audit, retention: current-plus-history }
  public.schema_migrations: { kind: table, domain: ops, classification: ledger, grain: one applied migration, status: canonical, ownerRole: tradzfx_migrator, pitPolicy: operational-only, retention: permanent }
  public.setup_evaluations: { kind: table, domain: strategy, classification: audit, grain: one setup evaluation per candidate/context, status: canonical, ownerRole: tradzfx_strategy, pitPolicy: immutable-decision, retention: long-term }
  public.strategy_families: { kind: table, domain: strategy, classification: configuration, grain: one strategy family/base specification, status: canonical, ownerRole: tradzfx_strategy, pitPolicy: snapshot-required, retention: long-term }
  public.strategy_settings_snapshot: { kind: table, domain: strategy, classification: snapshot, grain: one immutable strategy settings hash, status: canonical, ownerRole: tradzfx_strategy, pitPolicy: immutable-snapshot, retention: long-term }
  public.strategy_signal_candidates: { kind: table, domain: strategy, classification: audit, grain: one strategy candidate/decision observation, status: canonical, ownerRole: tradzfx_strategy, pitPolicy: immutable-decision, retention: partitioned-plus-summary }
  public.strategy_specs: { kind: table, domain: strategy, classification: legacy-configuration, grain: one versioned monolithic strategy specification, status: legacy, ownerRole: tradzfx_strategy, pitPolicy: snapshot-required, retention: until-reference-cutover }
  public.strategy_variants: { kind: table, domain: strategy, classification: configuration, grain: one strategy family override variant, status: canonical, ownerRole: tradzfx_strategy, pitPolicy: snapshot-required, retention: long-term }
  public.zone_outcomes: { kind: table, domain: analysis, classification: fact, grain: one evaluated outcome per zone/event/horizon, status: canonical, ownerRole: tradzfx_backtest, pitPolicy: source-anchor-bound, retention: partitioned-long-term }
  public.zone_touch_event_refresh_state: { kind: table, domain: ops, classification: state, grain: one checkpoint per zone-touch producer scope, status: canonical, ownerRole: tradzfx_lifecycle, pitPolicy: operational-only, retention: current-state }
  public.zone_touch_events: { kind: table, domain: market, classification: event-ledger, grain: one immutable touch per zone/event and touch timestamp, status: canonical, ownerRole: tradzfx_lifecycle, pitPolicy: anchor-bounded, retention: partitioned-feature-history }
