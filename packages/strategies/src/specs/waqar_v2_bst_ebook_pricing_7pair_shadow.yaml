id: waqar_v2_bst_ebook_pricing_7pair_shadow
familyId: waqar_v2
active: false
name: "Waqar Asim Scalp — BST ebook-pricing seven-pair shadow"
version: "3.1.0-shadow.ebook-pricing.1"
description: >
  Inactive counterfactual clone of waqar_v2_bst_corrected_7pair_shadow.
  Changes only pricing interpretation: bullish bias requires discount and
  bearish bias requires premium, matching waqar_ebook_v1. Valid only for
  ranges wholly inside British Summer Time.

filters:
  symbols: [ EURUSD, GBPUSD, AUDUSD, NZDUSD, USDCAD, USDCHF, USDJPY ]
  sessions: [ LONDON, OVERLAP ]
  timeWindows:
    - { utcStart: "07:00", utcEnd: "08:00" }
    - { utcStart: "13:00", utcEnd: "14:00" }

setup:
  - id: htf_zone
    feature: features_zone
    tf: 1h
    predicate: "zone_kind IN ('demand', 'supply') AND is_fresh = true"
    required: true
    groupBy: [ zone_kind ]

  - id: trend_bias
    feature: features_htf_bias
    tf: 15m
    predicate: "direction != 'neutral' AND state IN ('READY', 'SOFT_WARN')"
    required: true

  - id: premium_discount
    feature: features_pricing
    tf: 1h
    predicate: |
      (features_htf_bias.direction = 'bullish' AND position IN ('discount', 'deep_discount'))
      OR
      (features_htf_bias.direction = 'bearish' AND position IN ('premium', 'deep_premium'))
    required: true

entry:
  - id: structure_break
    feature: features_structure
    tf: 1m
    predicate: "event_type IN ('bos', 'mss', 'choch') AND direction = features_htf_bias.direction"
    required: true
    groupBy: [ direction ]
    ignoreLifecycle: true

  - id: zone_retest
    feature: features_zone
    tf: 1m
    predicate: "zone_kind IN ('demand', 'supply', 'fvg') AND is_fresh = true AND fill_pct <= 1.0 AND direction = features_htf_bias.direction"
    required: false
    groupBy: [ zone_kind ]

  - id: displacement_confirm
    feature: features_displacement
    tf: 1m
    predicate: "grade IN ('MEDIUM', 'HIGH') AND direction = features_htf_bias.direction"
    required: false

signalSource: zone

risk:
  sl: "5 pips"
  tp: "sl * 3.0"
  minRR: 3.0
  timeoutBars: 180
  maxFillBars: 45

gates:
  - id: spread_gate
    name: spread
    params:
      maxSpreadPips: 3.0

  - id: session_gate
    name: session
    params:
      allowed: [ LONDON, OVERLAP ]

  - id: portfolio_heat
    name: portfolioHeat
    params:
      maxConcurrentPerSymbol: 1
      maxConcurrentTotal: 2

live:
  mode: disabled
  lotSize: 0.01
  riskPerTradePct: 1.0
  accountBalance: 10000
  accountCurrency: USD
  signalTtlMinutes: 15
  maxSpreadPips: 3.0
  maxSlippagePoints: 20
  entryZonePips: null
  maxPositionsPerSymbol: 1
  maxPositionsTotal: 2
  cooldownMinutes: 30
  structureFreshnessMinutes: 180

notes:
  - "Research-only counterfactual; never promote live."
  - "Only pricing predicate differs from waqar_v2_bst_corrected_7pair_shadow."
  - "Bullish discount / bearish premium matches waqar_ebook_v1 interpretation."
  - "07:00-08:00 and 13:00-14:00 UTC equal authored London windows during BST."
