{
  "name": "@tm/analyzer-backtest",
  "version": "1.0.0",
  "private": true,
  "description": "Analyzer backtest harness: simulate setup evaluations over historical candles and grade-calibrate outcomes.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "vitest run"
  },
  "dependencies": {
    "@tm/shared": "workspace:*",
    "@tm/setup-engine": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vitest": "^1.0.0"
  }
}
